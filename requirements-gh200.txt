# Requirements for GH200 / IQ HPC (gh-aria) — ARM64 (aarch64)
# Install with: pip install -r requirements-gh200.txt --index-url https://download.pytorch.org/whl/cu128
#
# IMPORTANT: torch and torchvision MUST be installed with --index-url
# to get CUDA-enabled ARM64 wheels. Bare `pip install torch` on ARM64
# silently installs a CPU-only build.
torch>=2.7.0
torchvision>=0.22.0
torchattacks==3.5.1
scikit-learn>=1.3.2
scipy>=1.10.1
numpy<2.0
matplotlib
pillow
tqdm
optuna>=4.1.0
sqlalchemy>=2.0
rmm-cu12>=24.0
git+https://github.com/samueleblanc/knowledgematrix.git@0d26c7a2959b8845bd370874c195a2b30c553688
