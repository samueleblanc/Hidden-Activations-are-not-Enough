[pytest]
# importlib mode avoids the package-name collision between
# unit_test/km_feature_viz/ and the real km_feature_viz/ package.
# Without this, pytest adds unit_test/ to sys.path, causing the shadow
# __init__.py to take precedence over the real package.
addopts = --import-mode=importlib
testpaths = unit_test
